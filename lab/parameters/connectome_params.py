"""Per-neuron / per-edge parameter specs (phase 3)."""

from __future__ import annotations

from lab.parameters.registry import ParameterRegistry


def register_connectome_specs(registry: ParameterRegistry) -> None:
    return None
