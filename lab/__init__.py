"""C. elegans virtual lab backend (FastAPI).

Sibling of :mod:`celegans_live_demo`; shares the active-inference simulation
stack but exposes a richer control surface (parameter registry, body /
connectome introspection, per-tick inspection streams, transport control).
"""
